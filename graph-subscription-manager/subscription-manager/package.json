{
  "name": "subscription-manager",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ subscription-manager",
    "create-local": "graph create --node http://localhost:8020/ subscription-manager",
    "remove-local": "graph remove --node http://localhost:8020/ subscription-manager",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 subscription-manager",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.72.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
